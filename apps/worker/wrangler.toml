name = "social-media-scheduler-api"
main = "src/index.ts"
compatibility_date = "2026-09-01"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

[vars]
ENVIRONMENT = "production"
TIMEZONE = "Australia/Melbourne"
LIVE_TEST_CONFIRM = "false"
TOKEN_ENCRYPTION_KEY_VERSION = "v1"
META_GRAPH_VERSION = "v23.0"
META_APP_REVIEW_APPROVED = "false"
TIKTOK_CONTENT_POSTING_AUDITED = "false"
YOUTUBE_API_AUDIT_APPROVED = "false"

[[queues.producers]]
binding = "PUBLISH_QUEUE"
queue = "social-scheduler-publish"

[[queues.consumers]]
queue = "social-scheduler-publish"
max_batch_size = 5
max_batch_timeout = 10
# Application terminal/ambiguous outcomes are acknowledged. Only thrown
# infrastructure failures use these bounded retries before reaching the DLQ.
max_retries = 5
dead_letter_queue = "social-scheduler-dead-letter"

[triggers]
crons = ["* * * * *"]

[observability]
enabled = true
head_sampling_rate = 1
redact_query_string = true
